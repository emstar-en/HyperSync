{
  "catalogue_tools": [
    {
      "description": "Scan directory or file to add models to catalogue",
      "name": "catalogue_scan",
      "parameters": {
        "auto_tag": "boolean (automatically tag models)",
        "path": "string (file or directory path)",
        "recursive": "boolean (scan subdirectories)"
      },
      "returns": "list of model IDs"
    },
    {
      "description": "List catalogued models with optional filtering",
      "name": "catalogue_list",
      "parameters": {
        "filter": "dict (format, family_id, etc.)",
        "limit": "integer (max results)",
        "sort_by": "string (field to sort by)"
      },
      "returns": "list of model summaries"
    },
    {
      "description": "Get complete information about a model",
      "name": "catalogue_get",
      "parameters": {
        "model_id": "string (UUID)"
      },
      "returns": "complete model entry"
    },
    {
      "description": "Search models by name, capability, or tag",
      "name": "catalogue_search",
      "parameters": {
        "query": "string (search terms)"
      },
      "returns": "list of matching models"
    },
    {
      "description": "Get family tree for a model",
      "name": "catalogue_family",
      "parameters": {
        "model_id": "string (UUID)"
      },
      "returns": "family tree structure"
    },
    {
      "description": "Add or remove tags from a model",
      "name": "catalogue_tag",
      "parameters": {
        "add_tags": "list of strings",
        "model_id": "string (UUID)",
        "remove_tags": "list of strings"
      },
      "returns": "success status"
    },
    {
      "description": "Set nLD profile for a model",
      "name": "catalogue_set_nld",
      "parameters": {
        "instability_score": "float (0-1)",
        "model_id": "string (UUID)",
        "nld_level": "integer",
        "threat_level": "string (safe/low/medium/high/critical)",
        "training_domains": "list of domain info"
      },
      "returns": "success status"
    },
    {
      "description": "Get catalogue statistics",
      "name": "catalogue_stats",
      "parameters": {},
      "returns": "statistics object"
    }
  ],
  "guidance": {
    "best_practices": [
      "Always scan new models before use",
      "Tag models for easy organization",
      "Set nLD profiles for cross-domain models",
      "Track tuning history for internal models",
      "Use family trees to understand model evolution",
      "Verify model integrity before deployment"
    ],
    "nld_handling": [
      "nLD level 1: Safe, no special handling",
      "nLD level 2-3: Monitor for instability",
      "nLD level 4-6: Require authentication and containment",
      "nLD level 7+: Critical threat, strict isolation",
      "Always check instability score and threat level"
    ],
    "when_to_use": [
      "User wants to organize their model collection",
      "Need to track model lineage and tuning history",
      "Managing high-nLD models with security concerns",
      "Building model families through fine-tuning",
      "Searching for models with specific capabilities"
    ]
  }
}