# Desktop Deployment Guide (Docker Compose)

**Generated by STUNIR**
**Environment:** Desktop / Local Dev
**Version:** `{{ version }}`

## Overview

This guide covers running **{{ module_name }}** locally using Docker Compose. This is ideal for development, testing, or personal desktop usage.

## Prerequisites

*   Docker Desktop or Docker Engine `{{ docker_version | default(">= 20.10") }}`
*   Docker Compose `{{ compose_version | default(">= 2.0") }}`

## Configuration (.env)

The following environment variables can be configured in your `.env` file.

| Variable | Default | Description |
| :--- | :--- | :--- |
{% for var in env_vars %}
| `{{ var.name }}` | `{{ var.default | default("") }}` | {{ var.description | default("-") }} |
{% endfor %}

## Services

The stack consists of the following services:

| Service | Image | Ports |
| :--- | :--- | :--- |
{% for svc in services %}
| `{{ svc.name }}` | `{{ svc.image }}` | `{{ svc.ports | join(", ") }}` |
{% endfor %}

## Usage

1.  Create a `.env` file based on the table above.
2.  Start the stack:
    ```bash
    docker-compose up -d
    ```
3.  Access the service at: `http://localhost:{{ primary_port }}`
