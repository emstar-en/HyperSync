# Edge Device Deployment (Systemd/Binary)

**Generated by STUNIR**
**Environment:** Edge / IoT / Embedded
**Version:** `{{ version }}`

## Overview

This guide covers the lightweight deployment of **{{ module_name }}** on edge devices (e.g., Raspberry Pi, Jetson, Industrial Gateways).

## Hardware Requirements

*   **Arch**: `{{ architecture | default("aarch64 / x86_64") }}`
*   **RAM**: Min `{{ min_ram }}`, Rec `{{ rec_ram }}`
*   **Storage**: `{{ storage_req }}`

## Installation Steps

1.  **Place Binary**: Copy `{{ binary_name }}` to `/usr/local/bin/`.
2.  **Install Config**: Place `config.toml` in `/etc/{{ module_name }}/`.
3.  **Install Service**: Copy `{{ service_name }}.service` to `/etc/systemd/system/`.

## Configuration (config.toml)

Key parameters for edge optimization:

| Key | Default | Edge Note |
| :--- | :--- | :--- |
{% for param in edge_params %}
| `{{ param.key }}` | `{{ param.default }}` | {{ param.note }} |
{% endfor %}

## Service Management

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now {{ service_name }}
sudo systemctl status {{ service_name }}
```
