# Enterprise Air-Gapped Deployment Guide

**Generated by STUNIR**
**Environment:** High Security / Offline
**Version:** `{{ version }}`

## Overview

This document outlines the procedure for deploying **{{ module_name }}** in environments without internet access.

## Artifact Manifest

Ensure you have the following files on your transfer media:

| Filename | SHA256 Checksum | Description |
| :--- | :--- | :--- |
{% for file in artifacts %}
| `{{ file.name }}` | `{{ file.sha256 }}` | {{ file.description }} |
{% endfor %}

## Image Loading

Load the container images into your local registry or daemon:

```bash
# Load images
{% for img in images %}
docker load -i {{ img.filename }}
{% endfor %}

# Retag for local registry (Optional)
docker tag {{ original_registry }}/{{ image_name }}:{{ version }} {{ local_registry }}/{{ image_name }}:{{ version }}
docker push {{ local_registry }}/{{ image_name }}:{{ version }}
```

## Offline Configuration

1.  **License Key**: Place your signed `license.key` in `/etc/hypersync/`.
2.  **Trust Store**: Ensure internal CA certs are mounted to `/etc/ssl/certs`.

## Verification

Run the offline verification script:

```bash
./verify_integrity.sh --offline --manifest checksums.txt
```
