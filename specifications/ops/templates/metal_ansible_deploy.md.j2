# Bare Metal Deployment Guide (Ansible)

**Generated by STUNIR**
**Playbook:** `{{ playbook_name }}`
**Version:** `{{ version }}`

## Overview

This document details the bare metal deployment for **{{ playbook_name }}** using Ansible.

## Prerequisites

*   Ansible `{{ ansible_version | default(">= 2.9") }}`
*   Target OS: `{{ target_os | default("Ubuntu 22.04 LTS") }}`

## Inventory Variables

The following variables should be defined in your inventory file (`inventory.ini`) or `group_vars`.

| Variable | Default | Description |
| :--- | :--- | :--- |
{% for var in variables %}
| `{{ var.name }}` | `{{ var.default | default("Required") }}` | {{ var.description | default("-") }} |
{% endfor %}

## Roles

This playbook executes the following roles:

{% for role in roles %}
### {{ role.name }}
{{ role.description }}

**Tasks:**
{% for task in role.tasks %}
*   {{ task.name }}
{% endfor %}
{% endfor %}

## Execution

Run the playbook using the following command:

```bash
ansible-playbook -i inventory.ini {{ playbook_file }}
```
