# Kubernetes Deployment Guide (Helm)

**Generated by STUNIR**
**Chart:** `{{ chart_name }}`
**Version:** `{{ chart_version }}`

## Overview

This document details the Kubernetes deployment for **{{ chart_name }}** using Helm.

## Prerequisites

*   Kubernetes `{{ kube_version | default(">= 1.24") }}`
*   Helm `{{ helm_version | default(">= 3.0") }}`

## Values Configuration

The following table lists the configurable parameters of the {{ chart_name }} chart and their default values.

| Key | Type | Default | Description |
| :--- | :--- | :--- | :--- |
{% for val in values %}
| `{{ val.key }}` | `{{ val.type }}` | `{{ val.default }}` | {{ val.description | default("-") }} |
{% endfor %}

## Installation

To install the chart with the release name `my-release`:

```bash
helm install my-release {{ repository_url }}/{{ chart_name }}
```

## Resources

This chart deploys the following Kubernetes objects:

{% for res in resources %}
*   **{{ res.kind }}**: `{{ res.name }}`
{% endfor %}
