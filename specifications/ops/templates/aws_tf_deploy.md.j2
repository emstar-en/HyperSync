# AWS Deployment Guide (Terraform)

**Generated by STUNIR**
**Source Artifact:** `{{ artifact_id }}`
**Version:** `{{ version }}`

## Overview

This document details the AWS deployment for **{{ module_name }}**.
It is generated from the machine-optimized Terraform JSON artifacts.

## Prerequisites

*   Terraform `{{ terraform_version | default(">= 1.0.0") }}`
*   AWS Provider `{{ aws_provider_version | default(">= 4.0") }}`

## Configuration Variables

The following variables are exposed by this module. Ensure these are defined in your `terraform.tfvars` or passed via environment variables.

| Name | Type | Default | Description |
| :--- | :--- | :--- | :--- |
{% for var in variables %}
| `{{ var.name }}` | `{{ var.type }}` | `{{ var.default | default("Required") }}` | {{ var.description | default("No description provided.") }} |
{% endfor %}

## Resources

This deployment will provision the following AWS resources:

| Resource Type | Name |
| :--- | :--- |
{% for res in resources %}
| `{{ res.type }}` | `{{ res.name }}` |
{% endfor %}

## Outputs

After application, the following outputs will be available:

| Name | Description |
| :--- | :--- |
{% for out in outputs %}
| `{{ out.name }}` | {{ out.description | default("No description.") }} |
{% endfor %}

## Usage

```hcl
module "{{ module_name }}" {
  source = "./{{ module_source_path }}"

  # Required Variables
{% for var in variables if not var.default %}
  {{ var.name }} = "..."
{% endfor %}
}
```
