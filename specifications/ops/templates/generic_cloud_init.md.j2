# Generic Network Deployment (Cloud-Init)

**Generated by STUNIR**
**Environment:** Generic VM / Cloud Instance
**Version:** `{{ version }}`

## Overview

This guide details how to bootstrap **{{ module_name }}** on any standard Linux VM using Cloud-Init (user-data).

## Supported Distributions

*   Ubuntu 20.04+
*   Debian 11+
*   Fedora 35+
*   CentOS Stream 9

## User-Data Configuration

The generated `user-data` script performs the following actions:

1.  **Package Installation**: Installs dependencies: `{{ packages | join(", ") }}`.
2.  **User Creation**: Creates service user `{{ service_user }}`.
3.  **Binary Download**: Fetches artifacts from `{{ artifact_url }}`.
4.  **Service Registration**: Enables systemd units.

## Customization

You can inject the following variables into the `write_files` section of the cloud-config:

| Path | Description |
| :--- | :--- |
{% for file in config_files %}
| `{{ file.path }}` | {{ file.description }} |
{% endfor %}

## Launch Example (Multipass)

```bash
multipass launch --name hypersync-node --cloud-init user-data.yaml
```
