apiVersion: control.hypersync.io/v1
kind: ControlManifest
metadata:
 name: basic-deployment
 namespace: default
 labels:
 app: web-server
 env: development
spec:
 intent: deploy
 target:
 type: service
 name: web-frontend
 resources:
 requests:
 cpu: "2"
 memory: "4Gi"
 tokens: 500000
 limits:
 cpu: "4"
 memory: "8Gi"
 tokens: 1000000
 scheduling:
 priority: 50
 preemptible: false
