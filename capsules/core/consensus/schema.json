{
    "type": "object",
    "properties": {
        "db_path": {
            "type": "string",
            "default": "consensus.db",
            "description": "Path to the SQLite database file"
        },
        "log_level": {
            "type": "string",
            "enum": ["DEBUG", "INFO", "WARNING", "ERROR", "CRITICAL"],
            "default": "INFO",
            "description": "Logging level for the consensus manager"
        },
        "default_consensus_mechanism": {
            "type": "string",
            "description": "ID of the default consensus mechanism to apply if none specified"
        },
        "default_attestation_protocol": {
            "type": "string",
            "description": "ID of the default attestation protocol to apply if none specified"
        }
    },
    "required": ["db_path"]
}