{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "seed": {
      "type": "integer",
      "default": 0,
      "description": "Seed for deterministic operations"
    },
    "max_steps": {
      "type": "integer",
      "default": 5,
      "description": "Maximum number of steps in a program execution"
    }
  }
}