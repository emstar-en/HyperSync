{
    "type": "object",
    "properties": {
        "playbooks_dir": {
            "type": "string",
            "default": "playbooks",
            "description": "Directory containing playbook markdown files"
        },
        "log_level": {
            "type": "string",
            "enum": ["DEBUG", "INFO", "WARNING", "ERROR"],
            "default": "INFO"
        }
    },
    "required": ["playbooks_dir"]
}