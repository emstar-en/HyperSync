{
    "type": "object",
    "properties": {
        "sector_id": {
            "type": "string",
            "description": "Unique identifier for the geometric sector managed by the Coordinator"
        },
        "policy_config": {
            "type": "object",
            "properties": {
                "global_gamma": {
                    "type": "number",
                    "default": 0.95
                },
                "initial_phase": {
                    "type": "string",
                    "enum": ["Pathfinder", "Cartographer", "Trailblazer"],
                    "default": "Pathfinder"
                }
            }
        }
    },
    "required": ["sector_id"]
}